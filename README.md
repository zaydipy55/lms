Firstly, we install VS code and python with setup 
intsalled virtual environment( pip install virtualenv)
Setup virtual environment ( python -m venv venv)
 pip install Django 
 Created Project named lms
 
